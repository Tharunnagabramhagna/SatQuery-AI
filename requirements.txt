# ==============================================================================
# SatQuery AI — Unified Python Dependencies
# ==============================================================================

# Core Backend Framework
fastapi>=0.110.0
uvicorn[standard]>=0.28.0
pydantic>=2.6.0
pydantic-settings>=2.2.0
python-multipart>=0.0.6
httpx>=0.27.0

# Database
sqlalchemy>=2.0.0
psycopg[binary]>=3.1.0
alembic>=1.13.0

# Authentication & Security
argon2-cffi>=23.1.0
PyJWT>=2.8.0

# AI / VLM — Gemini
google-genai>=2.0.0
tenacity>=8.2.0

# Image Processing
pillow>=10.0.0
numpy>=1.26.0

# VLM Inference (optional — uncomment if GPU/local model is available)
# torch>=2.2.0
# torchvision>=0.17.0
# transformers==4.40.0
# accelerate>=0.28.0

# GIS Pipeline (optional — uncomment if rasterio is available)
# rasterio>=1.3.11
# geopandas>=0.14.0
# shapely>=2.0.0
# pyproj>=3.6.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
